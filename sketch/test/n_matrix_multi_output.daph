//already worked for daphne original pass

//n = rand($dim, $dim, 1.0, 2.0, 0.2, -1);
//m = rand($dim, $dim, 1.0, 2.0, 0.2, -1);

n = rand($dim, $dim*2, 1.0, 2.0, 1, -1);
m = rand($dim, $dim*2, 1.0, 2.0, 1, -1);

f = n + 50;
z = f * m;

print(z);
print(f);